object Globals {
    object CallHandler {
        const val GROUP_ID = "io.github.singlerr.semaphore.callhandler"
    }

    object Controller {
        const val GROUP_ID = "io.github.singlerr.semaphore.controllers"
    }

    object DataGateways {
        const val GROUP_ID = "io.github.singlerr.semaphore.datagateways"
    }

    object Interactor {
        const val GROUP_ID = "io.github.singlerr.semaphore.interactors"
    }

    object Presenter {
        const val GROUP_ID = "io.github.singlerr.semaphore.presenters"
    }

    object View {
        const val GROUP_ID = "io.github.singlerr.semaphore.views"
    }
}